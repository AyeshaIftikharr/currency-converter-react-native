import { THEME, BLUE, GREEN, ORANGE, PURPLE } from '../theme';

export const theme = {
  BLUE: {
    primaryColor: BLUE,
    ...THEME,
  },
  ORANGE: {
    primaryColor: ORANGE,
    ...THEME,
  },
  PURPLE: {
    primaryColor: PURPLE,
    ...THEME,
  },
  GREEN: {
    primaryColor: GREEN,
    ...THEME,
  },
};
