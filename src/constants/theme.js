import { BLUE, GREEN, ORANGE, PURPLE } from '../theme';

export const THEME_TYPES = {
  BLUE: {
    enum: 'BLUE',
    color: BLUE,
  },
  GREEN: {
    enum: 'GREEN',
    color: GREEN,
  },
  ORANGE: {
    enum: 'ORANGE',
    color: ORANGE,
  },
  PURPLE: {
    enum: 'PURPLE',
    color: PURPLE,
  },
};
